<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".Activities.Notification"
            android:exported="false" />
        <activity
            android:name=".Activities.UserDetailPage"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".Activities.SearchResultPage" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".Activities.ProductPage"
            android:exported="false" />
        <activity
            android:name=".Activities.IntroPage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- <activity -->
        <activity
            android:name=".Activities.SearchResultPage"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".Activities.FavoritePage"
            android:exported="true" />
        <activity
            android:name=".Activities.Post"
            android:exported="true" />
        <activity
            android:name=".Activities.PrivateMenuActivity"
            android:exported="true" />
        <activity
            android:name=".Activities.PrivateChat"
            android:exported="true" />
        <activity
            android:name=".Activities.HomePage"
            android:exported="true" />
        <activity
            android:name=".Activities.UserPage"
            android:exported="true" />
        <activity
            android:name=".Activities.LoginPage"
            android:exported="true">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
    </application>

</manifest>